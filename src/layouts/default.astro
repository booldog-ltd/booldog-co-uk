---
import { Contact } from '../sections/index/contact/contact.tsx';

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="We’re an efficient and harmonious team of capable developers and
        designers with the strategy, tools, and skills to build comprehensive
        and intuitive digital products."
    />
    <title>{title}</title>
    <link rel="icon" href="/assets/icon192.png" />
    <style lang="scss" global>
      @use 'reset-css/sass/_reset';

      * {
        box-sizing: border-box;
      }
      body {
        font-family: Arial, Helvetica, sans-serif;
        scroll-behavior: smooth;
      }
    </style>
  </head>
  <body>
    <slot />
    <Contact client:idle />
  </body>
</html>
